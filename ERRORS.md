# ERRORS.md

2回を超える試行錯誤が必要だった問題の記録。効かなかった方法・効いた方法・次回の教訓。

---

## ETF銘柄の名称検索対応（2026-08-16）

レビュー4巡を要した。毎巡「1つ直すと別の経路が壊れる」を繰り返した。

### 問題の本質

検索の名称解決は **個別株・ETF・米国ティッカーの三つ巴の優先順位問題**だった。
当初「ETFが検索できない」という単一の欠陥に見えたが、実際は3者の境界をどう引くかの設計問題で、
片方の境界を動かすと必ず反対側が壊れる構造だった。

### 効かなかった方法

1. **`CURATED_STOCK_ALIASES` への別名追記**
   このテーブルは `generated.stocks.map()` の中で当該コードの `searchTerms` にマージされるだけ。
   生成JSONに存在しないコード（＝ETF）を追記しても map がそのコードを通らず**何も起きない**。
   「別名を足すだけの小改修」という当初の見立ては構造上成立しなかった。

2. **双方向の部分一致マッチング**
   「クエリがETF名の部分文字列」という逆方向を許すと、ETF名に含まれるASCIIトークン単体が
   ETFコードに解決される。`GOLD`（Barrick Gold）や `MSCI`（MSCI Inc.）という**実在の米国銘柄**が
   日本のETFに乗っ取られ、ユーザーに誤りと分からない形で別銘柄が表示された。

3. **先頭一致（最初にマッチしたものを即return）**
   配列順に依存して後発エントリが前のエントリに食われる。
   `2630`（為替ヘッジ**あり**）を正式名称で検索すると `2558`（ヘッジ**なし**）が返った。
   為替ヘッジの有無は別商品であり、金融アプリとして誤答の質が悪い。

4. **実データ表記への無条件追従**
   J-Quantsの実表記に合わせて銘柄名に全角マイナス（U+2212）を入れたら、
   自アプリの `searchSchema` がASCIIハイフンしか許可しておらず**公式名称の貼り付けが400**になった。
   NFKCはU+2212をU+002Dに畳まない（畳まれるのはU+FF0Dのみ）。
   さらに1545はASCIIで打ち直すと今度はnullになり、**どちらの綴りでも到達不能**という悪化を招いた。

5. **`isLikelyPlainUsTicker` を言及マッチより前に置く**
   米国ティッカー形状のクエリを早期リターンさせたら、`TOYOTA`/`SONY`/`NTT`/`MUFG` など
   **個別株の英字別名37件が404**になった。masterに対する退行だった。
   `SONY` は米国ADRが存在するため404にならずドル建てADRが返り、より気づきにくい壊れ方だった。

### 効いた方法

1. **順方向マッチのみ ＋ 最長一致 ＋ 曖昧ならnull**
   「エントリの名称/別名がクエリの中に現れる」の一方向だけを見る。
   複数マッチ時は一致termが最長のものを採用し、最長が複数コードで並んだらnullを返して既存経路に委ねる。
   これで逆方向の乗っ取りと順序依存の非決定性が同時に消え、
   2630が2558の名称を真部分文字列として含む関係も正しく解決された（長い方が勝つ）。

2. **`isLikelyPlainUsTicker` は言及マッチの後に置き、非個別株の解決だけを抑止する**
   言及マッチ自体は常に実行する。米国ティッカー形状のときに抑止するのはETF/非equity候補のみ。
   これで `GOLD`/`MSCI`（言及マッチなし→ETF抑止で素通し）と
   `TOYOTA`/`SONY`（言及マッチあり→個別株）が両立した。**この並びは不変条件**。

3. **`isShadowedMatch` を資産種別をまたいで一元的に効かせる**
   案2でETF/REITをマスタに収録した際、`iシェアーズ・コアJリートETF` が個別株「コア」(2359) に
   奪われる衝突が発生した（`・` U+30FB がScript=Commonで日本語ワード境界をすり抜けるため）。
   例外テーブルを足すのではなく、既存の「長いtermが短いtermを覆う」ロジックを
   個別株とファンドをまたいで1本の走査で効かせることで、実測99件の複数マッチを衝突0件にした。

4. **4種の総当たりテストを実装前に書く**
   案2ではこれを最初に行い、1巡で通った。

### 次回の教訓

1. **名称解決の変更には4種の総当たりテストを必ず先に書く。**
   - (a) 個別株 → 自コード（社名＋全別名）
   - (b) ETF/ファンド → 自コード（正式名称＋全別名）
   - (c) 相互奪取ゼロ（個別株↔ファンドの両方向）
   - (d) 全銘柄名が `searchSchema.safeParse` を通過する

   4巡の手戻りは、この4種が揃うまで毎回どれか1つを壊していたことが直接原因。
   特に **(d) は「単体テストは緑なのに実HTTPは400」という乖離を唯一検知できる**。
   ラウンドトリップテストは解決関数を直接呼ぶためバリデーション層を通らない。

2. **バリデーション層とデータ層の文字集合は必ず突き合わせる。**
   実データの表記に合わせるときは、その文字が自アプリの入力バリデーションを通るかを同時に確認する。
   案2では全銘柄名の文字種を集計して許可文字集合と差分を取り、
   **ETF収録以前から存在した13件の既存バグ**（`Ｊ．フロント　リテイリング` 等が400になる）も発見できた。

3. **テストが「あること」と「効くこと」は別。ミューテーションテストで確かめる。**
   レビュアーが実際にバグを注入して `TOYOTA` が404になりテストが赤くなることを実証した。
   逆に、米国ティッカーの検証が `GOLD`/`MSCI`（素通しが正解のケース）だけで、
   「素通ししてはいけないケース」のassertionが無かったため、
   テストのタイトルがバグ側の挙動を仕様として固定してしまっていた。

4. **「衝突しないよう手動確認した」という自己申告は信用しない。**
   実際には2組の衝突が残っており、2銘柄が自分の正式名称で到達不能だった。
   総当たりを機械的に回して初めて分かる。**申告する数値は必ず実測に基づかせる。**

5. **入力バリデーションを緩める前に、その文字が外部APIのURLに素通しされないか確認する。**
   `&` を許可しようとした際、`jquants.ts` の `getData()` がURLを文字列連結で組み立てており
   `code` パラメータが未エンコードだった。`extract4()` は4桁数字が無いとクエリ原文をそのまま返すため、
   `&` によるクエリパラメータ注入が成立する経路が実在した。正規表現を緩める前に修正した。
